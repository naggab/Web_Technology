{
  "name": "web_technology",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^5.3.0",
    "prettier": "^2.1.2",
    "webpack": "^5.3.0"
  },
  "scripts": {
    "check-codestyle": "prettier --check .",
    "fix-codestyle": "prettier --write .",
    "start:client": "yarn --cwd packages/client run start",
    "start:server": "yarn --cwd packages/server run start",
    "start": "concurrently --kill-others -n client,server \"yarn run start:client\" \"yarn run start:server\""
  }
}
